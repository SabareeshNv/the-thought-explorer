---
import { Picture } from "@astrojs/image/components";
import FormattedDate from "./FormattedDate.astro";
import { slugify } from "@utils/helpers";

const { post } = Astro.props;
const { title, summary, pubDate, category, image } = post.data;
---

<article
    class="grid md:grid-cols-3 lg:gap-2 bg-gray-50 shadow rounded-t md:rounded-l md:rounded-tr-none"
>
    <div class="md:col-span-2">
        <a href={`/blog/${post.slug}/`}>
            <Picture
                src={image.src}
                alt={image.alt}
                widths={[200, 400, 800, 1200]}
                sizes="(max-width: 800px) 100vw, 1200px"
                aspectRatio="2:1"
                format="webp"
                class="h-full cover rounded-t md:rounded-l md:rounded-tr-none"
            />
        </a>
    </div>

    <div class="p-6">
        <div class="flex flex-wrap items-center justify-between">
            <a
                href={`/category/${slugify(category)}`}
                class="text-sm tracking-wider font-semibold text-purple-900 uppercase"
                >{category}</a
            >

            <FormattedDate
                date={pubDate}
                class="text-sm text-gray-900 tracking-wider font-semibold"
            />
        </div>
        <h2 class="py-4 text-2xl font-semibold leading-snug font-heading">
            <a href={`/blog/${post.slug}/`}>{title}</a>
        </h2>
        <div class="text-md">
            {summary}
        </div>
    </div>
</article>
